; AMI is used by the Spring Boot host process to update Custom device states
; for the eight BLF reminder slots.

[general]
enabled = yes
webenabled = no
bindaddr = 0.0.0.0
port = 5038

[phone-agent]
secret = phone-agent
read = command,originate
write = command,originate
