; Generated by scripts/phone-agent-dev.sh start.
; Dialplan for Phone Agent BLF slots and inbound phone intents.

[phone-agent-mvp]
exten => 701,1,Answer()
 same => n,Wait(1)
 same => n,Playback(demo-congrats)
 same => n,Wait(2)
 same => n,Hangup()

exten => 702,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

exten => 703,1,Answer()
 same => n,Playback(tt-monkeys)
 same => n,Hangup()

exten => 601,hint,Custom:phone-agent-slot-1

exten => 602,hint,Custom:phone-agent-slot-2

exten => 603,hint,Custom:phone-agent-slot-3

exten => 604,hint,Custom:phone-agent-slot-4

exten => 605,hint,Custom:phone-agent-slot-5

exten => 606,hint,Custom:phone-agent-slot-6

exten => 607,hint,Custom:phone-agent-slot-7

exten => 608,hint,Custom:phone-agent-slot-8

exten => 601,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=1)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/1/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-1)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=1&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 602,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=2)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/2/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-2)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=2&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 603,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=3)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/3/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-3)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=3&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 604,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=4)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/4/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-4)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=4&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 605,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=5)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/5/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-5)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=5&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 606,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=6)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/6/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-6)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=6&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 607,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=7)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/7/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-7)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=7&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => 608,1,Answer()
 same => n,Set(__PHONE_AGENT_SLOT=8)
 same => n,Set(TASK_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/slots/8/start" || true)})
 same => n,Set(__PHONE_AGENT_TASK_ID=${TASK_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_TASK_ID}" = ""]?no_task)
 same => n,Playback(phone-agent/slots/slot-8)
 same => n,Playback(phone-agent/prompts/reply-after-beep)
 same => n,Playback(beep)
 same => n,Set(RECORDING_START_RESULT=${SHELL(curl -fsS --max-time 2 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings/start?slot=8&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" >/dev/null && printf OK || true)})
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_TASK_ID}.wav,3,60,k)
 same => n,Hangup()
 same => n(no_task),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

exten => h,1,GotoIf($["${PHONE_AGENT_INBOUND_INTENT_ID}" != ""]?inbound_done)
 same => n,GotoIf($["${PHONE_AGENT_SLOT}" = "" | "${PHONE_AGENT_TASK_ID}" = ""]?done)
 same => n,System(curl -fsS --max-time 5 -X POST "http://host.docker.internal:8080/internal/asterisk/recordings?slot=${URIENCODE(${PHONE_AGENT_SLOT})}&taskId=${URIENCODE(${PHONE_AGENT_TASK_ID})}" || true)
 same => n,Goto(done)
 same => n(inbound_done),System(curl -fsS --max-time 5 -X POST "http://host.docker.internal:8080/internal/asterisk/inbound-intents/phone/recordings?intentId=${URIENCODE(${PHONE_AGENT_INBOUND_INTENT_ID})}" || true)
 same => n(done),NoOp(phone-agent h extension done)

exten => 0,1,Answer()
 same => n,Set(INTENT_ID_RAW=${SHELL(curl -fsS --max-time 2 "http://host.docker.internal:8080/internal/asterisk/inbound-intents/phone/start" || true)})
 same => n,Set(__PHONE_AGENT_INBOUND_INTENT_ID=${INTENT_ID_RAW})
 same => n,GotoIf($["${PHONE_AGENT_INBOUND_INTENT_ID}" = ""]?no_intent)
 same => n,Playback(phone-agent/prompts/inbound-intent)
 same => n,Playback(beep)
 same => n,Record(/var/spool/asterisk/phone-agent/recordings/${PHONE_AGENT_INBOUND_INTENT_ID}.wav,3,120,k)
 same => n,Hangup()
 same => n(no_intent),Playback(phone-agent/prompts/no-task)
 same => n,Hangup()

[phone-agent-ring]
exten => s,1,Answer()
 same => n,Playback(phone-agent/prompts/ring-phone)
 same => n,Hangup()
